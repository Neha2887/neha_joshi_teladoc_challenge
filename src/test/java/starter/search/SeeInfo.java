/*package starter.search;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.testng.Assert;

import net.serenitybdd.core.pages.PageObject;

public class SeeInfo extends PageObject{
	
	public void seeinfo(String term)
	{
		String bodyText = getDriver().findElement (By.xpath ("//*[contains(text(),'Add User')]")).getText();
	    Assert.assertTrue(bodyText.contains(term));
		
		
	}

}*/
